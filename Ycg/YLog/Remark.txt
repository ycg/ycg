2015-04-25
	 这次真的要把日志组建给完善掉


1.各个日志等级，如 DEBUG，ERROR，INFO
	不同的日志等级放在不同的文件中去

2.为了发挥日志的最佳性能，采用队列的方式进行批量写入

3.如果把日志保存到本地文件里面去，需要这几个功能
	A，可以在配置文件指定保存路径
	B，可以如果文件大于多少的时候会自动生成一个新的文件，默认是100M
	C，可以指定文件名的配置方式
	D，文件命名 日期 + 日志类型 + (在同一天相同类型日志被拆分了)

	★ 在频繁写日志的时候需要做队列写入